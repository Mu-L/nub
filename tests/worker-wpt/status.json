{
  "_doc": "Per-file WPT expectations for nub's Worker conformance gate. An entry is one of: {} (every subtest must pass), {skip:<reason>} (file not run — needs a DOM substrate nub has no business providing), or {fail:{expected:[<subtest names>], note:<why>}} (those subtests are documented, accepted divergences; every OTHER subtest in the file must still pass). expected-fail entries are DOCUMENTED divergences, not silent gaps — most are inherited from node:worker_threads and shared with native Node. The Blob-instanceof divergence (D2) is a real nub finding pending a maintainer document-vs-fix decision; see the note. An unexpected pass on an expected-fail subtest is reported (so the list can be tightened) but does not fail the run; an unexpected FAIL (regression) does.",
  "tests": {
    "webmessaging/MessageEvent.any.js": {},

    "webmessaging/MessageEvent-trusted.any.js": {
      "skip": "Both subtests assert MessageEvent.isTrusted===true. Node's worker_threads MessageEvent is isTrusted=false (verified: native Node is also false), so this is a Node-inherited divergence, not nub-specific. The BroadcastChannel subtest also needs an on-disk worker support file we do not vendor."
    },

    "webmessaging/MessagePort_initial_disabled.any.js": {
      "fail": {
        "expected": ["Untitled"],
        "note": "PORT AUTO-START (Node-inherited). The spec requires that addEventListener('message') WITHOUT start() must not deliver; nub follows node:worker_threads, which starts a port on any listener (workerd shares this). Documented in the worker FAQ. The file itself notes it duplicates message-channels/no-start.any.js."
      }
    },

    "webmessaging/MessagePort_onmessage_start.any.js": {},
    "webmessaging/Channel_postMessage_event_properties.any.js": {},
    "webmessaging/Channel_postMessage_target_source.any.js": {},
    "webmessaging/Channel_postMessage_clone_port.any.js": {},
    "webmessaging/Channel_postMessage_clone_port_error.any.js": {},
    "webmessaging/Channel_postMessage_DataCloneErr.any.js": {},
    "webmessaging/Channel_postMessage_ports_readonly_array.any.js": {},
    "webmessaging/Channel_postMessage_with_transfer_outgoing_messages.any.js": {},
    "webmessaging/Channel_postMessage_with_transfer_incoming_messages.any.js": {},
    "webmessaging/Channel_postMessage_with_transfer_entangled.any.js": {},

    "webmessaging/Channel_postMessage_Blob.any.js": {
      "fail": {
        "expected": ["Test Description: Messages can contain Blobs."],
        "note": "BLOB instanceof DIVERGENCE (D2 — MAINTAINER-OWNED, document-vs-fix). A Blob survives the clone with intact bytes + a 'Blob' constructor name, but `cloned instanceof globalThis.Blob` is false: nub's blob-url support (runtime/worker-blob-url.cjs) replaces globalThis.Blob with a transparent SUBCLASS to snapshot construction parts for blob: workers, while the structured-clone deserializer produces a NATIVE node:buffer Blob — which is not an instance of the subclass. The data round-trips correctly; only the identity check fails. Re-pointing the native Blob's prototype (as the polyfill already does for File) is a candidate fix but is a polyfill behavior change for the maintainer to decide."
      }
    },

    "webmessaging/message-channels/basics.any.js": {},
    "webmessaging/message-channels/close.any.js": {},

    "webmessaging/message-channels/no-start.any.js": {
      "fail": {
        "expected": ["Untitled"],
        "note": "PORT AUTO-START (Node-inherited). addEventListener('message') without start() should not deliver per spec; node:worker_threads (and workerd) start the port on any listener. Documented in the worker FAQ. Same divergence as MessagePort_initial_disabled.any.js."
      }
    },

    "webmessaging/message-channels/implied-start.any.js": {},
    "webmessaging/message-channels/dictionary-transferrable.any.js": {},

    "webmessaging/message-channels/worker-post-after-close.any.js": {
      "skip": "CLOSE() SEMANTICS divergence. These probe the browser's GRACEFUL self.close() — queued tasks (and a postMessage issued right after close()) still run/flush. nub's worker-scope close() is process.exit(0) (hard), so a postMessage after close() is lost and the test's async_test never completes (0 subtests). A documentable divergence; skipped because it cannot yield a meaningful pass/fail under a hard close."
    },

    "webmessaging/message-channels/worker.any.js": {
      "skip": "CLOSE() SEMANTICS divergence (same as worker-post-after-close): the blob: worker calls self.close() then postMessage(true); nub's hard process.exit(0) close drops the post, so the async_test never completes."
    },

    "html/infrastructure/safe-passing-of-structured-data/messagechannel.any.js": {
      "fail": {
        "expected": [
          "Blob basic",
          "Blob unpaired high surrogate (invalid utf-8)",
          "Blob unpaired low surrogate (invalid utf-8)",
          "Blob paired surrogates (invalid utf-8)",
          "Blob empty",
          "Blob NUL",
          "Array Blob object, Blob basic",
          "Array Blob object, Blob unpaired high surrogate (invalid utf-8)",
          "Array Blob object, Blob unpaired low surrogate (invalid utf-8)",
          "Array Blob object, Blob paired surrogates (invalid utf-8)",
          "Array Blob object, Blob empty",
          "Array Blob object, Blob NUL",
          "Array Blob object, two Blobs",
          "Object Blob object, Blob basic",
          "Object Blob object, Blob unpaired high surrogate (invalid utf-8)",
          "Object Blob object, Blob unpaired low surrogate (invalid utf-8)",
          "Object Blob object, Blob paired surrogates (invalid utf-8)",
          "Object Blob object, Blob empty",
          "Object Blob object, Blob NUL",
          "FileList empty",
          "Array FileList object, FileList empty",
          "Object FileList object, FileList empty",
          "ImageData 1x1 transparent black",
          "ImageData 1x1 non-transparent non-black",
          "Array ImageData object, ImageData 1x1 transparent black",
          "Array ImageData object, ImageData 1x1 non-transparent non-black",
          "Object ImageData object, ImageData 1x1 transparent black",
          "Object ImageData object, ImageData 1x1 non-transparent non-black",
          "ImageBitmap 1x1 transparent black",
          "ImageBitmap 1x1 non-transparent non-black",
          "Array ImageBitmap object, ImageBitmap 1x1 transparent black",
          "Array ImageBitmap object, ImageBitmap 1x1 transparent non-black",
          "Object ImageBitmap object, ImageBitmap 1x1 transparent black",
          "Object ImageBitmap object, ImageBitmap 1x1 transparent non-black",
          "An object whose interface is deleted from the global must still deserialize",
          "Growable SharedArrayBuffer",
          "ImageBitmap",
          "OffscreenCanvas"
        ],
        "note": "Structured-clone battery (152 subtests). The 38 expected fails sort into: (1) BLOB instanceof (the 19 'Blob …' rows) — the D2 dual-Blob divergence above, the clone produces a native Blob that is not an instance of nub's wrapped globalThis.Blob; data is intact. (2) DOM-only types with NO Node substrate — FileList/ImageData/ImageBitmap need `document`/canvas (throw 'document is not defined' / 'OffscreenCanvas is not defined'); these are legitimately out of scope (no DOM on Node) and stay expected-fail rather than skip so a future DOM polyfill would surface as an unexpected-pass. (3) 'interface deleted from the global' + 'Growable SharedArrayBuffer instanceof ArrayBuffer' — browser/spec edges (a growable SAB is not an ArrayBuffer instance under Node). EVERY non-Blob, non-DOM clone case (Map/Set/Date/typed arrays/SAB-identity/transfer/circular/…) PASSES."
      }
    },

    "workers/Worker-custom-event.any.js": {},
    "workers/Worker-replace-event-handler.any.js": {}
  }
}
